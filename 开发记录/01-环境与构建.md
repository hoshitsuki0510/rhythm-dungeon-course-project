# 环境与构建记录

更新时间：2026-08-24

## 开发机

- macOS 26.3.1，Apple Silicon `arm64`。
- Git 2.55.0、Homebrew 6.0.15、VS Code 1.133.0。
- Xcode Command Line Tools 与 Apple Clang 17 已安装。
- `ffmpeg`、`ffplay`、`ffprobe` 8.1.2 已安装。

## Godot 路线

- 版本：Godot 4.7.2 stable，标准版，GDScript。
- 安装来源：Godot 官方 macOS 下载页与官方发布镜像。
- 编辑器路径：`/Applications/Godot.app`。
- 编辑器版本：`4.7.2.stable.official.ed1daf0bf`。
- 编辑器架构：Universal 2，同时包含 `arm64` 与 `x86_64`。
- 编辑器安全验证：代码签名有效，满足 Designated Requirement；Gatekeeper 显示 `Notarized Developer ID`，发布者为 Prehensile Tales B.V.。
- 导出模板路径：`~/Library/Application Support/Godot/export_templates/4.7.2.stable/`。
- 已安装模板：macOS Universal 2、Windows x86_64 debug/release 及其 console wrapper、ICU 数据。
- 模板安装占用：约 325 MB。

## 计划交付格式

- macOS：`.dmg`，内部包含 Universal 2 `.app`。
- Windows：优先导出 x86_64 `.exe` 与同目录 `.pck`，随后整体打包为 `.zip`。
- `DMG` 和 `EXE` 不是互相转换；它们由同一个 Godot 项目使用不同平台的导出模板分别生成。

## 跨平台验收要求

- macOS 构建：在本机从 Finder 启动并完成一轮游戏。
- Windows 构建：必须在真实 Windows x86_64 电脑或可靠的 Windows 虚拟机上完成启动、输入、音频、存档路径和完整流程测试。
- 每次正式构建记录 Godot 版本、Git 提交、导出日期、目标平台和测试结果。

## 安装与验证记录

- 2026-08-24：从 Godot 官方页面确认当前稳定版为 4.7.2，选择标准版而非 .NET 版。
- macOS 编辑器包大小为 170,622,178 字节；官方 SHA-512 与本地结果均为 `38aa16e5bba2083941fc5b3e54be0089bd4cc35e32415f5b9fd9a8a6a7b9818255d44532ea8ef94b5aef56c4b407c2d634fa4f657e4ebe681ebbf59b7bac69ca`。
- 完整导出模板包大小为 1,281,349,702 字节；官方 SHA-512 与本地结果均为 `ca4d71c4d7b81dfc15d1a98baa07534aa95b03fdda78a0075b06672e1648d2e5f40980c9adc28d23e1b92e732ee7bf3461997aa804af74ec2fcd7a93ccb84079`。
- 两个压缩包均通过完整性测试；只从模板包提取项目需要的 macOS、Windows x86_64 和 ICU 文件，没有安装 Android、iOS、Linux 或 Web 模板。
- 安装后再次运行 `Godot --headless --version`，结果为 `4.7.2.stable.official.ed1daf0bf`；模板版本标记为 `4.7.2.stable`，两者匹配。
- 尚未创建正式 Godot 工程，因此尚未执行真实 `.dmg`/`.exe` 构建冒烟测试；该测试应在第一个可运行场景建立后立即进行。
