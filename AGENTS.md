# 小学期游戏项目协作约定

## 跨会话恢复

- 所有本项目新会话首先完整读取根目录 `RESUME.md`，再读取项目记忆和当前子任务相关证据。
- 恢复后先输出 `RESUME.md` 规定的五项摘要，确认唯一子任务后再修改文件。
- 已验收工作不重复执行；若实际文件、Git、构建或测试证据与恢复摘要冲突，以可复核的实际证据为准并修正记录。
- 每个小分任务仍按下方规则更新开发日志和项目记忆；阶段状态、风险或下一安全动作变化时，同步更新 `RESUME.md`。

## 进度与记忆

- 每完成一个边界清晰的小分任务，立即更新 `开发记录/开发日志.md`，并把稳定进度同步到本项目记忆。
- 项目记忆通过 Codex 受控记忆更新说明维护；不得保存密码、令牌或其他敏感信息，也不要把一次性调试输出写进长期记忆。
- 记忆中的状态必须以实际文件、命令结果、构建产物或测试证据为依据，不能把计划写成已完成。

## 问题记录

- 开发中遇到具有复用价值的问题时，及时写入 `开发记录/问题记录.md`。
- 每条问题至少记录：现象、影响范围、原因、解决方法、验证方式和是否仍有遗留风险。
- 纯粹的一次性拼写错误或没有复用价值的小失误无需进入问题记录。

## Terra 调度

- 对体量较大、边界独立、可单独验收的只读分析或已隔离写入任务，主动调用 `terra_worker`。
- 委派必须使用完整的无历史上下文，明确文件范围、非目标、预期输出和验收标准；主线程必须复核结果。
- 几分钟内即可完成、强依赖当前上下文或会与主线程产生文件冲突的任务留在主线程。

## 平台与交付

- 开发主机与主要演示版本为 macOS；Windows 目标为常见的 x86_64 桌面系统。
- Windows 实机测试由用户在另一台 Windows 电脑上完成；本项目不准备 Windows 虚拟机。
- 避免依赖仅支持 macOS 的原生插件；所有正式构建都要记录 Godot 版本、Git 提交和测试结果。
