# 小学期 2D 节奏闯关游戏项目恢复入口

更新时间：2026-08-24

> 本文件是所有本项目新会话的第一读取入口。聊天记录不是项目事实源；若聊天、记忆摘要或本文件与实际文件、Git 历史、构建产物或测试结果冲突，以可复核的实际证据为准。

## 项目身份

- 课程：两周小学期程序实践，以汇报答辩为主要评分方式。
- 方向：较完整的 2D 像素音乐节奏闯关游戏，参考“节拍与行动结合”思路，不复刻《节奏地牢》的完整系统。
- 技术路线：Godot 4.7.2 stable 标准版 + GDScript。
- 项目目录：本文件所在目录。
- 开发与演示：macOS 为主；Windows x86_64 由用户在另一台实体 Windows 电脑上测试，不准备虚拟机。
- 远程仓库：`https://github.com/hoshitsuki0510/rhythm-dungeon-course-project`，公开，默认分支为 `main`。
- 会话拆分：每个会话只负责一个边界清晰、可验收的子任务；阶段完成后可开启新会话。
- 当前阶段：开发前了解与范围收敛。工程基线已建立，正式节奏玩法尚未开始；在用户明确进入正式开发前，不自动实现玩法。

## 事实源优先级

出现不一致时，按以下顺序复核：

1. 实际工程文件、资源、构建产物与真实运行或实机测试结果。
2. Git 工作区、提交历史、分支与远程状态。
3. `开发记录/` 中的决策、日志、问题和里程碑证据。
4. 本文件的阶段登记和当前安全动作。
5. Codex 项目记忆中的本项目条目。
6. 旧会话中的口头摘要或未经验证的计划。

任何计划、候选方案、未测试功能和尚未在 Windows 实机运行的构建，都不得写成“已完成”。

## 新会话恢复顺序

1. 读取本文件 `RESUME.md`。
2. 读取项目级 `AGENTS.md`，再读取 Codex 项目记忆中的“小学期程序实践—2D 节奏闯关游戏”条目。若项目记忆尚未可用，不阻塞恢复；继续以本文件、Git 和项目证据为准，并在恢复摘要中标明。
3. 执行只读 Git 核对：`git status --short --branch`、`git log --oneline --decorate -8` 和 `git remote -v`。
4. 查看下方“阶段与子任务登记表”，确定本会话的唯一任务。
5. 只读取该任务相关的事实源：通常是 `开发记录/开发日志.md`、对应里程碑、相关问题记录和将要修改的工程文件；不应无目的地重读全部历史。
6. 在修改任何文件前，先向用户输出一份简短的“恢复摘要”；若 Git 存在未知的未提交改动，先识别并保留，不覆盖。
7. 完成子任务后，按“阶段交接清单”同步证据、记忆和 Git；只有在阶段状态或下一动作发生变化时才更新本文件。

## 恢复摘要格式

新会话应在开始工作前用不超过五条说清：

```text
当前阶段：
已验收基线：
本会话唯一子任务：
当前阻塞或未验证风险：
下一安全动作：
```

这份摘要只用于确认恢复结果，不替代开发记录和项目记忆。

## 已验证基线

- Godot `4.7.2.stable.official.ed1daf0bf` 标准版已安装，macOS Universal 2、Windows x86_64 和 ICU 导出模板与引擎版本匹配。
- Godot 工程、占位主场景、GDScript 入口、资源目录、导出预设和 Git 已初始化。
- 主场景无界面运行已输出 `INIT_SMOKE_OK`。
- macOS DMG 初始化冒烟构建已通过镜像、架构、签名和启动验证。
- Windows x86_64 EXE/PCK 已成功导出并完成静态结构检查，但尚未在 Windows 实机运行，不得标记为实机验收通过。
- 公开 GitHub 仓库、`origin` 和 `main` 跟踪关系已建立，普通 `git push` 已验证可用。
- `builds/`、Godot 缓存、导出凭据和本地 `课件/` 已被忽略，课程 PDF 未上传公开仓库。
- 正式节拍系统、角色移动、敌人、攻击、关卡和正式素材均尚未实现。

历史初始化场景、早期里程碑和旧日志中的“下一步制作节拍/移动原型”是当时的建议，已被用户后续确定的“先初步了解，再进入正式开发”取代。历史记录保留原样，当前动作以本文件为准。

## 当前目录结构

```text
./
├── RESUME.md                 # 跨会话恢复入口
├── AGENTS.md                 # 项目协作与记录约束
├── README.md                 # 对外项目说明
├── project.godot             # Godot 工程配置
├── export_presets.cfg        # macOS / Windows 导出预设
├── scenes/                    # Godot 场景
├── scripts/                   # GDScript 源码
├── assets/                    # 精灵、瓦片、音频和字体
├── 开发记录/                  # 决策、日志、问题和里程碑证据
├── builds/                    # 本地导出产物，Git 忽略
└── 课件/                      # 本地参考 PDF，Git 忽略
```

目录边界：源码和自制或已核实授权的项目资源才进入 Git；导出产物、凭据、Godot 自动缓存和课程 PDF 不进入公开仓库。

## 阶段路线图

以下只是开发阶段边界，不代表未开始的功能已经锁定：

1. 工程基础：引擎、导出、Git、GitHub、记录与恢复机制。
2. 开发前了解与范围收敛：Godot 基本概念、核心循环、节拍规则和最小可玩范围。
3. 核心节奏原型：固定 BPM 节拍信号、按格移动和节拍窗口判定。
4. 单关卡可玩闭环：攻击、一种敌人、受伤/死亡、胜利/失败与重开。
5. 内容与表现：像素素材、可用音频、UI、反馈、教程和必要的关卡内容。
6. 双平台交付与答辩：macOS/Windows 实机测试、构建、录屏、证据整理和汇报准备。

## 阶段与子任务登记表

| 子任务 ID | 模块 | 状态 | 事实源 | 已有产物 | 下一步 |
|---|---|---|---|---|---|
| BASE-001 | Godot 与导出环境 | `ACCEPTED` | `开发记录/01-环境与构建.md` | Godot 4.7.2、macOS/Windows 模板与安装验证 | 升级引擎或正式构建时复验 |
| BASE-002 | Godot 工程与跨平台冒烟构建 | `ACCEPTED` | `开发记录/里程碑/2026-08-24-工程初始化.md` | 占位场景、导出预设、DMG/EXE-PCK 初始化验证 | Windows 最终版仍需实机测试 |
| INFRA-001 | Git 与公开 GitHub 仓库 | `ACCEPTED` | `开发记录/01-环境与构建.md` | `main`、`origin`、公开远程、安全忽略规则 | 稳定节点持续提交和推送 |
| INFRA-002 | 跨会话恢复机制 | `EVIDENCE_REPORTED` | `RESUME.md`、`AGENTS.md` | 恢复顺序、事实源优先级、登记表与交接清单 | 下一个真实新会话按本文件恢复后，再升级为 `ACCEPTED` |
| LEARN-001 | 开发前了解与最小范围设计 | `EXECUTING` | 用户当前决定、`开发记录/00-项目决策.md` | 已确定方向和高层范围，未锁定具体节拍规则 | 用户完成初步了解后，单独确认核心循环和最小可玩范围 |
| LEARN-001A | Godot 基础概念速览 | `DRAFT` | `RESUME.md` 当前安全动作 | 无 | 作为默认首个微任务：只讲解场景、节点、脚本、信号和资源，不修改正式玩法 |
| PROTO-001 | 固定 BPM、按格移动与节拍窗口 | `DRAFT` | 尚未建立任务证据 | 无 | 只在用户明确进入正式开发后开始 |

状态使用：`DRAFT`、`APPROVED`、`EXECUTING`、`EVIDENCE_REPORTED`、`UNDER_REVIEW`、`ACCEPTED`、`REVISE`、`BLOCKED`。只有完成验证并留下可追溯证据的子任务才能进入 `ACCEPTED`。

## 跨会话统一原则

- 每个新会话先确认唯一子任务，不自行扩大到下一阶段。
- 已验收工作不重复执行；只有当实际证据冲突、环境漂移或当前任务必须依赖复验时才重新检查。
- 用户的现有修改和未跟踪文件不得被覆盖、删除或擅自发布。
- 游戏规则、素材、美术风格和范围变更必须区分“候选”与“用户已确认”。
- 音乐、音效、字体、精灵、瓦片和课程材料进入公开 GitHub 前必须确认授权和文件大小。
- 每个小分任务完成后更新开发日志和项目记忆；阶段或下一动作变化时再更新 `RESUME.md`。
- 遇到可复用问题时，在 `开发记录/问题记录.md` 写明现象、影响、原因、解法、验证和遗留风险。
- 适合的大型、独立、可验收任务使用 `terra_worker`；轻量任务保留在主会话，主线始终负责验收。

## 当前未锁定事项

- 游戏最终名称和一句话核心循环。
- BPM、节拍判定宽容度、踩错节拍的惩罚和移动/攻击的拍点关系。
- 关卡、敌人、教程和内容数量的最终上限。
- 音乐、音效、字体、精灵和瓦片的具体来源与授权。
- 答辩展示使用现场演示、录屏或两者并行。
- Windows 最终构建的真实运行结果。

## 阶段交接清单

一个阶段或边界清晰的子任务完成时：

1. 运行与风险相匹配的测试，将实际结果与完成标准对照。
2. 更新 `开发记录/开发日志.md`；重要阶段复制里程碑模板建立独立节点记录。
3. 如有可复用问题，更新 `开发记录/问题记录.md`。
4. 把稳定、已验证的进度同步到项目记忆，不记录凭据和一次性输出。
5. 检查 `git diff`、暂存区和授权边界，建立边界清晰的 Git 提交；在稳定节点推送 `origin/main`。
6. 当阶段状态、未验证风险或下一安全动作变化时，更新本文件的登记表和对应段落。
7. 结束时保证新会话不需依赖旧聊天内容，就能说清“已完成什么、证据在哪里、还有什么没验证、下一步是什么”。

## 当前下一安全动作

用户当前需要先进行初步了解，暂不进入正式玩法实现。新会话可以从下列任一边界清晰的任务开始：

- **默认首个微任务 `LEARN-001A`：**简要讲解 Godot 的场景、节点、脚本、信号和资源概念，不修改正式玩法代码。
- 把游戏构思收敛为一句话核心循环和“必做 / 可选 / 不做”范围。
- 确定节拍行动的最小规则，但只写设计说明，不立即写正式代码。

推荐的新会话开场请求：

```text
请继续小学期 2D 节奏闯关游戏项目。当前子任务是 `LEARN-001A`“Godot 基础概念速览”。请先读取 RESUME.md、AGENTS.md 和项目记忆，输出恢复摘要，只做概念讲解，不开始编写正式玩法代码。
```
